# M3 milestone — VariationalSNLDS core (`factored` encoder first)

**PRD:** [§9 Milestones](PRD-burn-port.md#9-milestones) (row **M3**), [§4 Scope](PRD-burn-port.md#4-scope).

**Status:** ✅ complete — merged to main 2026-04-29.

---

## Goal

Implement **burn `Module` / `Autodiff`**-compatible **VariationalSNLDS**-equivalent model matching Python [`VariationalSNLDS`](../identifiable-SDS/models/VariationalSNLDS.py) and supporting modules in [`modules.py`](../identifiable-SDS/models/modules.py): transition heads per discrete state, decoder, \(\pi\) and \(Q\), Gaussian latents with init/emission structure, **`inference='alpha'`** ELBO path using **`log_Z`**, and training-time tensors for reconstruction / KL / switching terms.

**Encoder policy:** Ship **`factored` (MLP)** encoder first for integration parity and tests; **`recurrent` (bi-LSTM → causal LSTM)** remains **stretch / follow-on** unless promoted into M3 ([PRD §4](PRD-burn-port.md#4-scope) encoder row). Prefer a clean **trait or enum** seam so **`encoder_type`** can grow without rewriting inference.

**Kernel dependency:** Discrete-state HMM messaging (**[M2](M2.md)**) and local-evidence plumbing should be **wired or duplicated minimally** so `_compute_elbo` / `_compute_posteriors` analogue can call the same numerics as Python.

---

## Crate layout (informative)

Align with PRD [§7 Technical design](PRD-burn-port.md#7-technical-design-high-level): new workspace member **`snlds-model`** (or equivalent) for Burn `Module` graphs; keep **`snlds-data`** IO-only; **`snlds-core`** may host shared numerics **or** fold into `snlds-model` — **pick one layout** in implementation and record it under **Updates** below.

---

## Completion checklist

- [x] Workspace member **`snlds-model`** (`crates/snlds-model/`) with `VariationalSnlds<B: Backend>` and `Mlp<B>` modules.
- [x] **K transition MLPs** (softplus, one per discrete state); **decoder** (leaky-ReLU, latent→obs); **`q_logits` [K,K]** and **`pi_logits` [K]** as `Param<Tensor>`; **temperature** scaling applied as `log_softmax(logits / temp, dim)`.
- [x] **Factored encoder** (leaky-ReLU, obs→2·latent_dim); reparameterised sample `z = μ + σ·ε`; diagonal covariance with `factor² + 1e-6` eps regularisation.
- [x] **`compute_local_evidence`**: `log p(z_0 | s=k)` under per-state init Gaussian + `log p(z_t | z_{t-1}, s=k)` via MLP transition means; calls M2 `log_forward` / `log_backward`.
- [x] **`compute_elbo`**: reconstruction `log N(x; x̂, σ²I)`, entropy `0.5·sum(1 + log_var + log 2π)`, MSM `= sum_t log_Z_t / N` for `inference='alpha'`; ELBO = recon + entropy + β·MSM.
- [x] **6 tests**: `forward_output_shapes`, `forward_loss_finite`, `forward_posteriors_sum_to_one`, `beta_zero_disables_msm`, `seq_len_one_exercises_init_only_branch`, `autodiff_gradient_smoke`.
- [ ] [docs/PRD-burn-port.md](PRD-burn-port.md) **§8.5** — no new first-party deps beyond Burn (already present).

---

## Testing requirements

Inherit **M0** gates: `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace`.

| Area | Requirement |
|------|-------------|
| **Forward + loss** | One **CPU** minibatch; **finite** ELBO / loss components; no NaN after reasonable init |
| **Shapes** | Batch / time / dim_latent / dim_obs / **K** match declared config |
| **Autodiff** | At least one **gradient** smoke (small parameter subset) comparable to **M0** autograd smoke |
| **Parity (optional)** | Stricter tests promoted when reference tensors exist |

**Out of scope for M3:** Rerun training dashboards (**[M-Viz+](M-Viz+.md)**); full **training CLI** polish (**[M4](M4.md)**); **NeuralMSM** (**[M5](M5.md)**); **recurrent** encoder unless explicitly moved into this milestone.

---

## Out of scope (milestone boundaries)

- **Production** training loop, **checkpoint** story, **Adam** + schedule as **default** user path — **[M4](M4.md)**.
- **Ground-truth-only** Rerun — **[M-Viz](M-Viz.md)**; **posteriors in Rerun** — **[M-Viz+](M-Viz+.md)**.
- **CNN / image** paths — **[M6](M6.md)** (stretch).
- **Bit-exact** RNG match with NumPy (see PRD **success criteria**).

---

## Downstream

**[M-Viz+](M-Viz+.md)** logs \(\gamma_{t,k}\), \(\hat{x}_t\), and training scalars once this model exposes them. **[M4](M4.md)** adds **train binaries**, schedules, and checkpoints around the same core.

---

## Updates

| Item | Description |
|------|-------------|
| Crate layout | New crate `snlds-model`; `snlds-core` hosts HMM kernels; `snlds-model` depends on both |
| Encoder | `factored` (MLP) only; recurrent encoder deferred |
| `Activation` import | Burn 0.20.1: `burn::nn::activation::{Activation, ActivationConfig}`, not `burn::nn::` |
| `log_softmax` | Free function `burn::tensor::activation::log_softmax(tensor, dim)`, not a method |
| Posteriors | `exp(log_alpha + log_beta)` sums to 1 by construction — no extra softmax needed |
| Field visibility | All `VariationalSnlds` and `Mlp` fields are `pub` (required for gradient smoke test) |

---

## Document history

| Date       | Note |
|------------|------|
| 2026-04-29 | Initial tracker (aligned with M0–M2 milestone docs). |
| 2026-04-29 | M3 complete and merged to main; checklist and Updates filled in. |
